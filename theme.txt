Frontend Skills and Technologies:
- Core: HTML5, CSS3 (Vanilla), Vanilla JavaScript (ES6+)
- Typography: Inter font family (Google Fonts)
- Frameworks: None (Pure Vanilla HTML/CSS/JS)

Design Elements & Aesthetics:
- Overall Style: Premium modern UI combining Glassmorphism and subtle Neomorphism.
- Color Palette: Premium base background (e.g., #d1d8e0), elevated white surface cards (#ffffff), and a Camel Brown primary accent (#C19A6B) with glowing variations.
- Glassmorphism: Semi-transparent backgrounds with deep backdrop blurs (backdrop-filter: blur) used on the navbar and content cards.
- Neomorphism (Soft UI): Multi-layered, soft shadows for cards giving them a raised, 3D feel, and deep inset shadows for form inputs to make them appear pressed into the surface.
- Ambient Backgrounds: Floating, animated "glow orbs" in the background using heavy blur filters and CSS keyframe animations.
- Dynamic Effects: Text gradients for headings, glowing box-shadows for primary buttons.
- Micro-animations: Fade-up entrance animations staggered across grid items, shimmering "processing" bars, and smooth transform/shadow transitions on hover.

--------------------------------------------------------------------------------

PROMPT TO REPLICATE THIS FRONTEND FOR A DIFFERENT WEB APP:

If you want an AI (like me) to build a different web app using this exact same frontend style, copy and paste the following prompt:

"""
Build a premium, modern frontend for my new web app using HTML, Vanilla CSS, and Vanilla JavaScript. Please follow this specific design system and aesthetic:

1. Core Tech: Use vanilla HTML/CSS/JS only. No Tailwind, Bootstrap, or React.
2. Typography: Use the 'Inter' font from Google Fonts (weights 300 to 900).
3. Design Style: Blend Glassmorphism (using `backdrop-filter: blur(20px to 40px)` on semi-transparent cards/navbars) with Neomorphism (using soft, multi-layered box-shadows for cards and deep inset shadows for form inputs).
4. Color Palette: Use a sophisticated light-gray/blue base background (e.g., #d1d8e0) with white card surfaces. Use a Camel Brown (#C19A6B) as the primary accent color for buttons, links, and highlights.
5. Background: Include animated, floating "ambient orbs" in the background (using border-radius 50%, large blur filters, and CSS keyframe animations to slowly move them).
6. Animations & Interactions: Include staggered fade-up entrance animations for cards, smooth hover transitions that lift elements and expand their shadows (or add a glow), and text gradients for major headings. 
7. Structure: Define all colors, shadows, and spacing using CSS variables (:root) at the top of the stylesheet for easy theming.
"""
